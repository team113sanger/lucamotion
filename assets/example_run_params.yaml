# Example project params (copy and edit for your run).
# Use with: nextflow run <repo> -params-file inputs/example_run_params.yaml -profile farm22

input_manifest: "inputs/sample_list.tsv"
reference_genome: "/lustre/scratch124/resources/dermatlas/GRCh38_full_analysis_set_plus_decoy_hla.fa"
experiment_file: "inputs/experiment.yaml"
library_file_directory: "libraries"
outdir: "./results"

run_luca_count: true
run_aggregate_counts: false

luca_count_mm_reads: true
luca_sort_mm_read_counts: true
luca_cpus: 8
luca_extra_args: ""

publish_dir_mode: "copy"
