nextflow_pipeline {

    name "Test Workflow main.nf"
    script "main.nf"

    test("Should run without failures") {

        when {
        params {
                load("$baseDir/tests/testdata/example_params.json")
            }
        }

        then {
            assert workflow.success
        }

    }

}
